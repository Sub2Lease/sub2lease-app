// Auto-injected by Vite's `define` config (or falls back to "default-version")
// Add this to your vite.config.ts:
//   define: { __COMMIT_HASH__: JSON.stringify(process.env.COMMIT_HASH ?? "") }

declare const __COMMIT_HASH__: string | undefined;